/**
 * alterable game variables
 * all variables in the game that are potentially changable
 * separating them out so that they are easy to find
 */

 var variables = {
 	numberOfAnimals	: 4,
    roundTime		: 130
 };
